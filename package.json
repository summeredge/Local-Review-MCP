{
  "name": "local-review-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "local-review-mcp": "dist/src/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "diagnose:conversation-routing": "npm run build && node dist/src/cli.js diagnose-conversation-routing",
    "diagnose:review-delivery": "npm run build && node dist/src/cli.js diagnose-review-delivery",
    "diagnose:browser-router": "npm run build && node dist/src/cli.js diagnose-browser-router",
    "diagnose:review-context": "npm run build && node dist/src/cli.js diagnose-review-context",
    "start": "node dist/src/cli.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.4"
  }
}
