{
  "_comment": "Copy this file to config.production.json and set the local values before starting.",
  "port": 12080,
  "workspace": "C:\\Your\\Project",
  "workspaces": [
    {
      "id": "your-project",
      "name": "Your Project",
      "path": "C:\\Your\\Project"
    }
  ],
  "auth": {
    "token": "",
    "_comment": "Set a local token; never commit the token."
  },
  "remote": {
    "enabled": true,
    "provider": "cloudflare",
    "tunnelName": "<tunnel-name-or-uuid>",
    "endpoint": "https://<public-hostname>/mcp",
    "_comment": "Use an existing Named Tunnel name or UUID and its stable public MCP endpoint."
  },
  "supervisor": {
    "enabled": true,
    "healthIntervalSeconds": 30,
    "maxRestartAttempts": 3
  }
}
